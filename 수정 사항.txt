아래 내용들 수정 사항 반영해주세요.

전사 공통 (모든 문서)

 용어 통일: “Model Server (AI API)”/“GPU Worker” → Inference Server, Training Server로 통일.

 내부 통신 인증 표기 통일: X-Internal-Token → Authorization: Bearer <INTERNAL_API_TOKEN> 로 전면 교체.

 파일명/대소문자 통일: CLAUDE.md 등 실제 파일명 기준으로 모든 문서의 표기 일치.

 문서 간 링크/앵커 점검: 한글 헤더 앵커 깨지는 곳은 영문 별칭 병기(예: ## 2. 인증 API (Authentication)), 또는 수동 앵커 추가.

TECHSPEC.md

 Webhook 경로를 실제 구현과 일치시킴:

변경: POST /api/styles/:id/callback → POST /api/webhooks/training/complete (필요 시 inference도 병기).

apps/*/GUIDE.md 표기를 **CODE_GUIDE.md**로 수정.

 용어 통일 반영: Inference/Training 명칭, 토큰/서명 등 표기 리뷰 후 일괄 반영.

각 루트 README.md
 구조 표 내 파일명 수정: GUIDE.md → CODE_GUIDE.md.

docs/API.md

 섹션 헤더에 영문 별칭 병기(앵커 안정화): 예) ## 2. 인증 API (Authentication).

 Webhook·진행률 경로를 Backend README와 완전히 동일하게 맞춤:

Training progress: 하나의 합의된 경로로 고정(예: PATCH /api/webhooks/training/progress 또는 PATCH /api/styles/:id 중 택1 → 모든 문서 동기화).

 내부 통신 인증 헤더 설명을 Bearer 방식으로 명확히 유지(다른 문서 교정 참조 링크 추가).

apps/backend/README.md

 API 섹션에 docs/API.md 링크 추가.

 Webhook/진행률 경로 표기 API.md와 일치 확인·수정.

 “13 models” 등 숫자 고정 표현은 주기 업데이트(모델 증감 시 함께 수정).

pps/training-server/README.md

 Webhook/진행률 경로 옆에 API.md 해당 섹션 링크 추가.

 하이퍼파라미터 표에 TECHSPEC/PLAN 참조 문구 추가(출처 명확화).

 CODE_GUIDE 참조 문구 추가: “구현 시 CODE_GUIDE.md 준수”.

apps/inference-server/README.md

 완료/실패 Webhook 경로 옆에 API.md 링크 추가.

 “서명(워터마크) 삽입” 용어를 TECHSPEC과 완전 동일 표현으로 통일.

CODE_GUIDE.md

Backend

 PATTERNS.md 참조 링크 명확화(중복 설명 최소화).

 예시 코드가 실제 모델/필드명과 어긋나면 갱신.

Frontend

 Axios 인터셉터/에러 처리 규칙을 TECHSPEC·PATTERNS와 맞춤.

 테스트 가이드(Vitest/Playwright) 최소 기준 1~2줄 추가.

Training / Inference

 로깅 레벨·주기(진행률 30초 등) TECHSPEC과 동일 문구로 명시.

 예외 처리 시 Backend failed 콜백 보장 규칙 명문화.

 모델/아웃풋 경로 규칙(파일명, S3 경로) API 명세와 일치 확인.

 (Inference) 모델 로딩 정책(프로세스 시작 1회 로드 등) 명시.